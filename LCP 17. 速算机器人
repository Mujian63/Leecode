class Solution:
    def calculate(self, s: str) -> int:
        
        
        
        x = 1
        y = 0 
        for f in s:
            if f == 'A':
                x = 2*x+y
            else:
                y = 2*y+x
        return x+y
